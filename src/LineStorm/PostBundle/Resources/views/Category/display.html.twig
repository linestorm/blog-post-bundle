{% extends 'LineStormCmsBundle::layout.html.twig' %}

{% block title %}Articles About {{ category.name }}{% endblock %}

{% block page_title %}
    {{ category.name }} Articles
{% endblock %}

{% block head %}
    {{ parent() }}

    <link rel="stylesheet" href="{{ asset(assetsPath ~ '/css/cms/post/list.css') }}" />
{% endblock %}

{% block body %}
    <h1>Category: {{ category.name }}</h1>

    {% if posts|length %}
        <div class="posts-container">
            {% include 'LineStormPostBundle:Post:list.html.twig' %}
        </div>
    {% else %}
        <div class="center-block no-posts">
            <h2 class="text-muted">There are no posts in this category yet</h2>
        </div>
    {% endif %}
{% endblock %}
